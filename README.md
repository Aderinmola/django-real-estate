#### A Django and React project 

#### It contains Celery, for background worker tasks

#### Django, djangorestframework, PostgreSQL, for DB Redis, for Celery Features 
#### Dockerized api, PostgreSQL, Nginx

#### MakeFile


#### To start up, type
#### -- make build
#### To stop docker, type
#### -- make down
